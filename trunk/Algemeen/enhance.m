% enhance(img_data)
% img_data = 3D RGB matrix containing pixel data
% Returns: normalised & segmented image with lift- and person-segments
function enhanced = enhance(img_data)
    % PLACEHOLDER:
    enhanced = joinchannels('rgb', img_data);