% classify(img)
% img = Labeled DIPImage
% Returns: Int array of length 2. First element is the # of persons in
%          the lift. Second element is the # of persons out the lift
function info = classify(img)
    % PLACEHOLDER:
    info = [2,2];